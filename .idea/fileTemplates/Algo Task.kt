#if (${PACKAGE_NAME} && ${PACKAGE_NAME} != "")package ${PACKAGE_NAME}
#end

/**
 * Author: ${USER}
 * Created: ${DATE} ${TIME}
 * Task: ${NAME}
 * Task link: 
 *
 * Algorithm complexity: 
 * Memory complexity:
 * Time to solve:
 *
 * Description:
 *
 * Notes:
 */